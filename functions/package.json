{
  "name": "scalar-api-watcher-functions",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "@types/node": "^22.0.0",
    "@types/node-fetch": "^2.6.0"
  },
  "private": true
}
